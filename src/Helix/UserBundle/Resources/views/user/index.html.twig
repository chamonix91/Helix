{% extends '@HelixBackoffice/Back/layout.html.twig' %}

{% block content %}
<div class="table-agile-info">
    <div class="panel panel-default">
        <div class="panel-heading">
            Liste des utilisateurs
        </div>
        <div class="row w3-res-tb">
            {#<div class="col-sm-5 m-b-xs">
                <select class="input-sm form-control w-sm inline v-middle">
                    <option value="0">Bulk action</option>
                    <option value="1">Delete selected</option>
                    <option value="2">Bulk edit</option>
                    <option value="3">Export</option>
                </select>
                <button class="btn btn-sm btn-default">Apply</button>
            </div>#}
            <div class="col-sm-4">
            </div>
            <div class="col-sm-3">
                <div class="input-group">
                    <input type="text" class="input-sm form-control" placeholder="Search">
                    <span class="input-group-btn">
            <button class="btn btn-sm btn-default" type="button">Go!</button>
          </span>
                </div>
            </div>
        </div>
        <div class="table-responsive">

    <table class="table table-striped b-t b-light">
        <thead>
            <tr>
                <th>Voir Détails</th>
                <th>Email</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Bloquer </th>
                <th>Passer à Premium </th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td><a href="{{ path('user_show', {'id': user.id}) }}"> <i class="fa fa-eye"></i></a></td>
                <td>{{ user.email }}</td>
                <td>{{ user.nom }}</td>
                <td>{{ user.prenom }}</td>
                <td align="center">
                    <a href="{{ path('ban_user', {'id': user.id}) }}"><i class="fa fa-lock"></i></a>
                    <span class="text-ellipsis"></span>
                </td>
                <td align="center">
                    {% if  user.type == 'free' %}
                        <a type="button" class="btn btn-success" href="{{ path('toPremium', {'id': user.id }) }}">To Premium </a>

                    {% endif %}                    <span class="text-ellipsis"></span>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        </div>
        <br><br><br><br><br><br><br><br>
        <footer class="panel-footer">
            <div class="row">

                <div class="col-sm-5 text-center">
                    {#<small class="text-muted inline m-t-sm m-b-sm">showing 20-30 of 50 items</small>#}
                </div>
                <div class="col-sm-7 text-right text-center-xs">
                    <ul class="pagination pagination-sm m-t-none m-b-none">
                        <li><a href=""><i class="fa fa-chevron-left"></i></a></li>
                        <li><a href="">1</a></li>

                        <li><a href=""><i class="fa fa-chevron-right"></i></a></li>
                    </ul>
                </div>
            </div>
        </footer>
    </div>


</div>



{% endblock content %}