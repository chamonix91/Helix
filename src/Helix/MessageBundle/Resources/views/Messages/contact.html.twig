{% extends '@HelixMessage/layout.html.twig' %}

{% block container %}

    <h2>Nouveau message à {{ user.username }}</h2>

    <form action="{{ url('fos_message_thread_new') }}" method="post">
        {{ form_widget(form) }}

        <input type="submit" />
    </form>

{% endblock %}
